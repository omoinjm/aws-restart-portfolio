{
  "name": "aws-restart-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:public": "node scripts/sync-public-content.mjs",
    "dev": "npm run sync:public && astro dev",
    "build": "npm run sync:public && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.6.0",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.7"
  },
  "devDependencies": {
    "@types/node": "^22.15.3"
  }
}